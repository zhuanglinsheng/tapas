let Point = types::make_type('x': types::Int, 'y': types::Float)
let Status = types::enum('Pending', 'Delivered')
let Request = types::make_type('point': Point, 'status': Status)
return {'Point': Point, 'Request': Request}
