let temporary: Int
temporary = 3
print(temporary)

var captured: Int
captured = 4
let read_captured = () -> Int {
    return captured
}
print(read_captured())

let local: String
local = 'ready'
print(local)

let left: Int, right: Int
left = 5
right = 6
print(left + right)
