/// fibonacci.tap
///
/// 用递归和迭代两种方式计算 Fibonacci 数列。
/// 递归函数计算单项，迭代函数生成指定长度的数列。
/// 示例输出 fib(10) 和前十二项，展示递归调用、循环与列表。

function fibonacci_recursive(n: Int) -> Int
{
    if (n < 2) {
        return n
    }
    return this(n - 1) + this(n - 2)
}

function fibonacci_sequence(count: Int) -> List[Int]
{
    let sequence: List[Int] = []
    let previous = 0
    let current = 1

    for (let i in 0 to count) {
        sequence.append(previous)
        // Update both consecutive values only after saving the emitted term.
        let next = previous + current
        previous = current
        current = next
    }
    return sequence.copy()
}

print('fib(10) = ', fibonacci_recursive(10))
pprint('first 12 Fibonacci numbers: ', fibonacci_sequence(12))
