// Breadth-first traversal of a graph represented by an adjacency dictionary.
// Time: O(V + E); auxiliary space: O(V).

var breadth_first_search = (
        graph: Dictionary[String, List[String]],
        start: String,
) -> List[String] {
    let queue: List[String] = [start]
    let visited: List[String] = [start]
    let order: List[String] = []

    while(queue.len() > 0){
        let vertex = queue[0]
        queue.pop(0)
        order.append(vertex)

        for(let neighbor in graph[vertex]){
            if((neighbor in visited) == false){
                // Mark on enqueue so a vertex never enters the queue twice.
                visited.append(neighbor)
                queue.append(neighbor)
            }
        }
    }
    return order.copy()
}

let graph: Dictionary[String, List[String]] = {
    'A' : ['B', 'C'],
    'B' : ['A', 'D', 'E'],
    'C' : ['A', 'F'],
    'D' : ['B'],
    'E' : ['B', 'F'],
    'F' : ['C', 'E'],
}

sprint('BFS from A: ', breadth_first_search(graph, 'A'))
